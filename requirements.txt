# PHI-Enhanced RLM v2.0 Requirements
# ===================================

# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
openai>=1.0.0
python-dotenv>=1.0.0

# Embeddings
sentence-transformers>=2.2.0  # Local embeddings (free, ~400MB)

# Vector Database
chromadb>=0.4.0               # Embedded vector store for large-scale retrieval

# Web extraction (better than regex)
trafilatura>=1.6.0            # Readability-style content extraction
# readability-lxml>=0.8.1     # Alternative: Mozilla's readability

# Document support
PyMuPDF>=1.23.0               # PDF extraction (import as fitz)
python-docx>=1.0.0            # Word document extraction

# REST API
fastapi>=0.109.0              # API framework
uvicorn>=0.27.0               # ASGI server
pydantic>=2.5.0               # Data validation

# Progress and UI
rich>=13.7.0                  # Beautiful terminal output
colorama>=0.4.6               # Cross-platform colors

# Async support
aiohttp>=3.9.0                # Async HTTP client
aiofiles>=23.2.0              # Async file operations

# Caching (SQLite is built-in)
# No additional deps needed for SQLite cache

# Optional: Tree-sitter for syntax-aware code parsing
# tree-sitter>=0.20.0         # Requires compilation
# tree-sitter-python>=0.20.0
# tree-sitter-javascript>=0.20.0

# Development/testing
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# httpx>=0.25.0               # For testing FastAPI
